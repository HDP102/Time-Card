Here's this week. Most of it was FireMon. Got shell access to the FMOS servers finally and ran the diagnostics support needed for the upgrade ticket, then worked through their license conversion script — it shipped with a placeholder host that had to be pointed at our own app server before it would run, so that took some back and forth, but the output is with Giuseff now and 180658 is moving. Also nailed down the z_firemon lockout that's been blocking the Cisco devices. It traced back to one ASA that never onboarded properly and has been retrying its log monitoring every ten minutes since June, which is why the account kept re-locking within half an hour of every unlock. Seth confirmed on his side that the devices authenticate to AD and the account is out of sync, and Sid and Seth between them identified five devices in my failure list that are already decommissioned, so the real footprint is smaller than the numbers suggested.

Zscaler I've now taken as far as I can. Ruled out credentials entirely — the ZIA account is enabled, I updated the current API key, DNS resolves, and there's no proxy misconfigured on the collector. It still times out at the TCP layer, and the same request returns cleanly from a normal workstation, so it's an egress path problem on the collector rather than anything on the Zscaler or FireMon side. Aksa is checking whether she has the updated PAC file. Where that leaves things is that most of what's outstanding now sits with other teams — the Azure client secret, the AD reset, the collector egress, and the upgrade itself with support. I also helped out with peer reviews through the week.

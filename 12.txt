Hi Swapnil, update on draft submission.

The Tufin GraphQL API we've been using (SecureTrack) can only create drafts. It has no submit, advance, or close operation. It also doesn't return a draft ID, so we can't point at a draft after it's created.

Submitting and closing tickets happens in SecureChange, which has its own REST API. That's the route for both of your asks: moving drafts to the next step, and auto-processing/closing. I'm checking two things now:
1. Whether SecureChange REST can see or submit the existing 123 drafts. If it can't, the cleaner path is to create the recert tickets directly through REST, already submitted, and cancel the drafts.
2. Whether ticketAPI can authenticate to SecureChange REST.

I'm also asking Leonard, since his workflow modules already use that API. I'll get you the 5 test tickets as soon as that's confirmed.

One thing to decide before we auto-close: Tufin would show ticketAPI completing each recertification, not the rule owner, and the owners' decisions only live in the spreadsheets. Since some of these are NERC rules, can you confirm that's acceptable evidence for audit?



Hi Leonard, quick SecureChange question.

My recert script uses the SecureTrack GraphQL createTicketDraft mutation. It created 123 drafts in prod under ticketAPI, on the "RLM Rule Recertification Workflow". Swapnil wants them submitted and ideally processed and closed by automation. GraphQL has no submit or close operation, so I'm moving to the SecureChange REST API, which I believe your workflow modules use.

A few questions:
1. Can SecureChange REST list or submit drafts? They don't show a ticket ID in the UI.
2. If not, what does the ticket payload look like for the RLM Rule Recertification workflow? In particular, how are the rules referenced? I'd create the tickets directly and cancel the drafts.
3. For advancing steps and closing: do you complete tasks through the steps/tasks endpoints, and are there workflow-specific fields I need to fill?
4. Does ticketAPI work with basic auth against /securechangeworkflow/api/securechange/, or do your scripts authenticate differently?

If you have a snippet from your modules you can share, that would save a lot of time. Thanks!

Hi Victor, disable tickets through the API fail in prod. createTicketDraft with workflowType: DECOMMISSION_RULES, workflowName: "MP RLM Rule Decommission/Disable", decommissionRulesAction: DISABLE_RULES passes with dryRun: true, but with dryRun: false returns RUD_1, 'There is no workflow with the defined name for this workflow type.' It fails the same way with a single rule. The workflow is Active in SecureChange, and Swapnil confirmed it's the right one. Recertify works fine on 'RLM Rule Recertification Workflow' with the same ticketAPI account. Is the MP decommission workflow set up for API-created drafts, or does ticketAPI need to be added as a requester?

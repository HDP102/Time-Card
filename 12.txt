Subject: Cisco IOS devices authenticate but no configuration is retrieved

Description:

Post-upgrade, retrievals against our Cisco IOS routers are not producing revisions. Example device is n01pubrtr001 at 10.16.1.7, managed by data collector V01SECAPL067 at 10.16.108.147.

The service account z_firemon authenticates successfully. I verified this manually over SSH from a workstation and can log in without issue. The credentials in FireMon were updated with the same password.

Two things I'm seeing. First, when I manually trigger a retrieval from FireMon, nothing appears in the router's authentication log from the collector's source address — no success and no failure. The only entries from 10.16.108.147 are older failed logins from before the password was updated. That suggests the retrieval attempt isn't reaching the device.

Second, when I log in interactively with that account it lands at privilege level 3, and show running-config returns only 167 bytes. I understand level 15 is required for a full config retrieval, so that may be a second issue behind the first.

Can you help determine whether the retrieval is being attempted at all, and what logs on the collector would show the attempt and any failure.

Worth attaching the Device Health Report output for that device if the form allows it.
